import { CallHandler, ExecutionContext, Injectable, Logger, Param, Req } from '@nestjs/common';
import { Observable } from 'rxjs';

@Injectable()
export class AppService {

  findAll(): any {
    return {};
  }
}
